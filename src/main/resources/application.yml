
server:
  error:
    include-message: always
    include-binding-errors: always
  port: 8080
  address: 0.0.0.0
  contextPath: /uaa


security.oauth2.client.clientId: acme
security.oauth2.client.clientSecret: acmesecret
security.oauth2.client.authorized-grant-types: authorization_code,refresh_token,password
security.oauth2.client.scope: openid

logging:
  level:
    root: INFO

keycloak:
  realm: staffect
  auth-server-url: http://130.61.252.200:8180/
  resource: spring-app
  bearer-only: true
  ssl-required: NONE

spring:
  datasource:
    password: ${SPRING_DATASOURCE_PASSWORD:docker}
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/postgres}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://accounts.google.com
          jwk-set-uri: https://www.googleapis.com/oauth2/v3/certs
          scope: openid,profile,email
      client:
        registration:
          google:
            clientId: 42661904383-0r3dvpam6r98t31j43ak63fmrqtmsi1k.apps.googleusercontent.com
            clientSecret: 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
